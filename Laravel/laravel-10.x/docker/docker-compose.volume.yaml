version: "3.8"
services:
  laravel-app:
    container_name: laravel10-volume
    build:
      context: .
      dockerfile: Dockerfile.volume
    image: laravel10:volume
    ports:
      - "8074:80"
    volumes:
      - ../:/app
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./supervisord.conf:/etc/supervisord.conf:ro
    environment:
      - APP_ENV=local
    command: >
      /bin/sh -c "
      cd /app &&
      /usr/bin/supervisord -c /etc/supervisord.conf
      "
